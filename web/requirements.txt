# parallax-web runtime dependencies.
# Install with: pip install -r requirements.txt (inside your venv).

# Anthropic SDK — HoP agent loop
anthropic>=0.40.0

# FastAPI + ASGI server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# SSE streaming (replaces manual text/event-stream writes)
sse-starlette>=1.8.0

# Request/response validation
pydantic>=2.0

# Storyboard contact sheets (tools/storyboard.py)
Pillow>=10.0

# YAML manifest parsing (used by parallax CLI subprocesses inheriting this env)
PyYAML>=6.0

# ElevenLabs voiceover (called by `parallax voiceover` subprocess)
elevenlabs>=1.0

# WhisperX phoneme-level forced alignment (called by `parallax transcribe` and
# auto-run inside `parallax voiceover`). Torch is a large install — NOT included
# by default. Install manually if you need voice transcription:
#   pip install whisperx
# whisperx

# Google Gemini image generation (called by `parallax create`)
google-genai>=0.3
