"""parallax_web — localhost chat + gallery frontend for Parallax.

This package is a thin shim around web/server.py so that
`python3 -m parallax_web` works when PYTHONPATH includes the web/ directory.
"""
